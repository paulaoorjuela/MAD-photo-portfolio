import { Routes } from '@angular/router';
import { MainComponent } from './Home/main/main.component';

export const routes: Routes = [
  { path:"", title:"Portfolio | Home", component: MainComponent}
];
